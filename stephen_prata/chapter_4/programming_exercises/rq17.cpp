#include <vector>
#include <array>
#include<string>

int main() {
    const int MAX_SIZE = 10;
    std::vector<std::string> vectorClass(MAX_SIZE);
    std::array<std::string, MAX_SIZE> arrayClass;

    return 0;
}